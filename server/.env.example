# —— Moji server 环境变量样板 ——
# 把这个文件复制成 .env，再把值填进去。
# .env 已被根目录 .gitignore 挡住，绝不会进 GitHub。绝不要把真 key 写进这个 .example 文件。
#
# 使用哪个 LLM provider：mock（默认，不花钱）或 claude（真 Claude API）
MOJI_LLM=claude

# Anthropic Claude API key（形如 sk-ant-...）。仅 MOJI_LLM=claude 时需要。
ANTHROPIC_API_KEY=your-key-here
